# MicroGameJamDegginger

Start node server by running server.js

Add web interface by hosting index.html
or
Add serial touch interface by running __main__.py with arguments 'serial port' and 'server ip'
